#define GLIBCXX_DEBUG
#include <iostream>
#include <vector>

using namespace std;

#define REP(i, n) for(int i=0; i < (int)n; ++i)
#define FOR(i, c) for(__typeof( (c).being())i = (c).begin(); i != (c).end(); ++i )
#define ALL(c) (c).begin(), (c).end()

int main()
{
  
}
